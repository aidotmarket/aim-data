# T-2026-000757: Published dataset detail state

The detail view now treats the server-side `listing_id` as proof of publication, even with an empty MarketplaceContext registry and no legacy marketplace data. The published badge and Marketplace tab render and the publish action is hidden. With only an ID, the tab shows a minimal published state and the listing ID; it omits unavailable sales statistics and legacy management actions. Zero values in known marketplace data remain zero.

The readiness response has no listing URL field. The listing link uses the existing success-toast route, `https://ai.market/listing/{encoded listing_id}`.

After successful disclosure snapshot completion, ListingPreparation registers the submitted title, description, tags and price through MarketplaceContext.publishDataset before refreshing the dataset. Disclosure retry and refresh-failure behavior remain covered.

## Validation

Base: untouched `origin/main` at `9a1f436c58c9d0f12d0908b585a1ea04ef112756`.
Branch: `build/t-2026-000757-published-view-listing-id`.
Environment: Node v25.6.0, npm 11.8.0; dependencies installed with `npm ci --no-audit --no-fund` separately in both worktrees.

| Check | Result | Failure set |
| --- | --- | --- |
| Full frontend suite, branch | 6 files, 37 tests passed | Empty |
| Full frontend suite, untouched main | 6 files, 35 tests passed | Empty |
| Production frontend build | Passed | Empty |
| ESLint, changed TSX files | Passed | Empty |
| git diff --check | Passed | Empty |

Suite command in each frontend directory: `rtk proxy sh -c 'npm test -- --reporter=json --outputFile=<report path> > <log path> 2>&1'`.
Build: `rtk npm run build`. Focused lint: `rtk proxy sh -c 'npx eslint src/pages/DatasetDetail.tsx src/pages/DatasetDetail.test.tsx'`.

Regression coverage renders the routed DatasetDetail with an empty real MarketplaceProvider: a non-null listing ID shows Published, hides Publish, and opens the minimal Marketplace tab; a null ID retains ListingPreparation and its Publish action. The existing completion test also verifies that MarketplaceContext records publication.

Build warnings concern the existing Browserslist data age and bundle size. No release was cut and main was not modified.
